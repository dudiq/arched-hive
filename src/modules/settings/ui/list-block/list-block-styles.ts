import { styled } from '@linaria/react'

export const IconWrapper = styled.div`
  min-width: 32px;
  display: flex;
  justify-content: center;
  opacity: var(--opacity-overflow);
`

export const LastNode = styled.div`
  padding-right: 10px;
`
